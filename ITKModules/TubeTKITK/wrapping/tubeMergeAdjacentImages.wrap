itk_wrap_include( tubeMergeAdjacentImages.h )

itk_wrap_named_class("tube::MergeAdjacentImages" tubeMergeAdjacentImages POINTER)
 foreach(t ${WRAP_ITK_SCALAR})
    foreach(d ${ITK_WRAP_IMAGE_DIMS})
      itk_wrap_template("${ITKM_${t}}${d}"  "${ITKT_${t}}, ${d}")
    endforeach()
 endforeach()
itk_end_wrap_class()
