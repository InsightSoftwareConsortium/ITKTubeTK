PROJECT( Blend )

CMAKE_MINIMUM_REQUIRED(VERSION 2.6)

FIND_PACKAGE( ITK )
IF( ITK_FOUND )
  INCLUDE( ${ITK_USE_FILE} )
ELSE( ITK_FOUND )
  MESSAGE( FATAL_ERROR "Cannot build without ITK." )
ENDIF( ITK_FOUND )

ADD_EXECUTABLE( Blend Blend.cxx )
TARGET_LINK_LIBRARIES( Blend ${ITK_LIBRARIES} )

