itk_wrap_include( tubeSegmentUsingOtsuThreshold.h )

itk_wrap_named_class("tube::SegmentUsingOtsuThreshold" tubeSegmentUsingOtsuThreshold POINTER)
 foreach(tI ${WRAP_ITK_SCALAR})
    foreach(d ${ITK_WRAP_IMAGE_DIMS})
      foreach(tM ${WRAP_ITK_SCALAR})
        itk_wrap_template("${ITKM_${tI}}${d}${ITKM_${tM}}"  "itk::Image<${ITKT_${tI}}, ${d}>, itk::Image<${ITKT_${tM}}, ${d}>")
      endforeach()
    endforeach()
 endforeach()
itk_end_wrap_class()
